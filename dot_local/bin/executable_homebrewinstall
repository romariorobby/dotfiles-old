#!/bin/bash
set -o nounset # error when referencing undefined var
set -o set errexit # exit when command fails

xcode-select --install

/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install.sh)" 

brew update


