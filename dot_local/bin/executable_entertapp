#!/bin/sh
cmds="\
🎵Spotify|1	spotify
🎥Kodi|2	kodi
🗃️TinyMM|	tinymediamanager
📚calibre|	calibre
🎥Otherapps|4	micsapp"

choice="$(echo "$cmds" | cut -d'	' -f 1 | dmenu -i)" || exit 1

`echo "$cmds" | grep "^$choice	"  | cut -d '	' -f2-`
